# Комплексный аудит Orda — 2026-06-27

4 параллельных аудитора (изоляция/безопасность, финансы, RBAC/надёжность, сверка прошлых аудитов + код сессии). Ниже — находки по приоритету и статус.

## ✅ Исправлено в быстром батче (2026-06-27)
- **packEdits в useMemo** (`store/purchase-plan/page.tsx`) — размер упаковки не пересчитывался сразу. Добавлен в deps.
- **null-org обход** — `catalog/[id]` PATCH и `purchase-plan/pack-size` PATCH: юзер без активной орг мог править чужие товары. Теперь 403 + всегда `.eq('organization_id', …)` для не-супера.
- **Слабый гейт прав `!!staffRole`** на наших новых роутах — заменён на `isStoreManager` (owner/manager/super) в: `ai/store-insights`, `ai/purchase-plan`, `store/purchase-plan/suggest`, `catalog/[id]` PATCH, `catalog/photo-upload`, `catalog/image-search`, `purchase-plan/pack-size`. Хелпер `lib/server/store-access.ts`.
- **Rate-limit** добавлен: `ai/copilot` (20/мин), `catalog/image-search` (30/мин), `catalog/photo-upload` (20/мин).
- **Таймзона «сегодня»** (CRITICAL деньги) — `dashboard` и `pos/ai-hint` считали день по UTC → теряли ~5ч вечерних продаж и врал % изменения. Теперь по Asia/Qyzylorda (`lib/core/time.ts` localDayISO/addDaysISO).

## 🔴 Осталось — КРИТИЧНО (деньги), отдельные проекты
- **Касса не сверяется при закрытии смены** (`point_shift_close`): нет `expected_closing_cash = opening + sales − returns − cash_expenses` и `difference`. Недостача/воровство по кассе невидимы. Объём: L (схема + RPC + UI).
- **Выручка не вычитает возвраты** — `dashboard`/`shifts/report` суммируют только `point_sales`, `point_returns` не вычитаются → выручка/касса завышены. Объём: M.
- **shifts/report by_hour** по серверному часу (UTC на Vercel) — гистограмма смещена на −5ч. Объём: S.

## 🔴 Осталось — БЕЗОПАСНОСТЬ
- **Kiosk RCE** — `spawn(gamePath)` по пути из неаутентифицированной broadcast/WS-команды под админом; канал `kiosk:{stationId}` публичный. Объём: L (релиз киоска).
- **Общие RBAC-таблицы** (`positions`/`role_capabilities`/`role_permissions`) без `organization_id` — владелец одной орг меняет роли/права во ВСЕХ. Объём: L.
- **`staff-accounts` POST** гейчен только `!!staffRole` — любой staff может invite/reset/changeEmail (по матрице — только супер). Объём: S (требует подтверждения поведения).
- **`salary/shift/[id]` GET** — зарплату видит любой staff (marketer). Объём: S.
- **`warehouse` POST** — приёмка (движение склада) гейчена `!!staffRole`. Объём: S.
- **Лимитер в памяти** (`rate-limit.ts`) — на Vercel не шарится между инстансами → лимиты слабые. Нужен Upstash/KV. Объём: M.
- **`admin-tokens` в памяти** — токен супер-API невалиден между инстансами (функц. баг). Объём: M.
- **`photo-upload`** — путь файла без `organization_id` префикса (bucket публичный, URL угадываем меж-тенант). Объём: S-M.

## 🟠 Осталось — прочее
- `xlsx ^0.18.5` (CVE) всё ещё в зависимостях (миграция на exceljs не завершена). M.
- Нет CSP-заголовка (HSTS есть). S-M.
- `store-insights` profit по текущему `sale_price`, а revenue по факту `total_amount` — могут расходиться при скидках/смене цены. S-M (продуктовое решение).
- `revenue4wPerWeek` мислейбл (это 7 дней, не 4 недели). LOW.
- `velThreshold` нижняя треть — кривой индекс для каталогов 1-2 SKU. LOW.
- `catalog/[id]` PATCH ловит «нет колонки» по тексту ошибки — лучше по `error.code='42703'`. LOW.
- `barcode-lookup` — мёртвая строка `void companyScope`. LOW.
- Cron `inventory-integrity`/`shortage-alert` фильтруют в приложении, но RPC агрегируют по всем компаниям — техдолг (утечка вернётся при вызове из нового места). MED.

## ✅ Подтверждено КОРРЕКТНЫМ (не баги)
- Изоляция прошлых аудитов реально закрыта (проверены 8 критичных точек: arena, kiosk login/qr, point/debts, profitability, purchase-plan и др.).
- `purchase-plan.ts` ядро заказа (demand/target/packs/amount) — без деления на 0, отрицательных, потери единиц.
- POS sale/return/discount/loyalty, недельная зарплата (net + по-компаниям), inclusive фильтры дат расходов/доходов.
- Код сессии в целом чистый (пагинация, мягкая деградация миграций, скоуп, loading/empty/error).
