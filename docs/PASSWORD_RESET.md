# Сброс пароля с телефона

Сотрудник меняет пароль не выходя из приложения: **Вход → «Не получается войти» → «Сменить пароль»**.

Два шага: рабочая почта → код из письма и новый пароль.

## Что нужно один раз настроить

Код в письмо добавляет Supabase, и по умолчанию его там нет — в стандартном
шаблоне только ссылка на сайт.

1. Supabase Dashboard → **Authentication → Emails → Reset Password**.
2. Добавить в тело письма строку с кодом:

   ```html
   <p>Код для смены пароля: <b>{{ .Token }}</b></p>
   <p>Код действует несколько минут и подходит только один раз.</p>
   ```

3. Ссылку `{{ .ConfirmationURL }}` из шаблона **не убирать**: она остаётся
   запасным путём для тех, кто меняет пароль на сайте.

Пока `{{ .Token }}` в шаблоне нет, в письме будет только ссылка. Приложение
это переживёт: ссылку можно **скопировать целиком и вставить в поле кода** —
внутри неё тот же одноразовый ключ, и проверка проходит так же. Но вставлять
ссылку неудобно, поэтому код в шаблон добавить стоит.

**SMTP тут ни при чём.** Он отвечает за то, от кого уходит письмо и дойдёт ли
оно (Supabase шлёт со своего адреса и с ограничением по частоте — для боевой
работы свой SMTP нужен). Что написано внутри письма, задаёт шаблон.

## Как устроено

| Шаг | Кто делает | Что происходит |
|-----|-----------|----------------|
| Запрос кода | `POST /api/auth/password-reset` `{action:"request"}` | сайт зовёт GoTrue `/recover`, письмо уходит |
| Смена пароля | `POST /api/auth/password-reset` `{action:"confirm"}` | код меняется на сессию (`/verify`), ею ставится пароль (`PUT /user`), сессия гасится |

Ключ проекта не уезжает в сборку: приложение знает только адрес сайта — так же,
как при входе (`/api/auth/mobile-session`).

**Существование почты наружу не подтверждается.** Ответ на запрос кода
одинаковый для заведённой и незаведённой почты — иначе форма превращается в
способ узнать, кто работает в компании.

Ограничения по IP: 5 писем и 10 попыток кода за 15 минут. Срок жизни кода и
число попыток проверяет сам GoTrue — свой счётчик был бы второй правдой.

## Смена известного пароля

Тому, кто пароль помнит, почта не нужна: **Аккаунт → «Сменить пароль»** (у
оператора — «Профиль → Пароль»). Текущий пароль спрашивается обязательно:
телефон бывает разблокирован и лежит на стойке, а смена пароля без
подтверждения — это захват учётной записи любым, кто взял его в руки.

`POST /api/auth/change-password` `{current_password, new_password}` — вход
проверяется тем же Supabase Auth (своей проверки пароля у нас нет и не должно
быть), пароль ставится сервисным ключом, флаг `must_change_password` снимается.

## Операторы

Сброс по почте оператору не подходит: он входит по логину, а почты у него
может не быть вовсе. В подсказке входа так и написано — забытый пароль
выдаёт владелец заново.

А вот **сменить** известный пароль оператор теперь может сам, в профиле: чаще
всего он и получает временный, сказанный вслух.
