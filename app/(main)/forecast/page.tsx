import { redirect } from 'next/navigation'

/**
 * /forecast слит в /analysis: там один прогноз на следующий месяц в трёх
 * сценариях, который сверяется с фактом и учится на ошибках. Раньше здесь
 * жил второй прогноз со своей формулой (сценарии ×0.75 / ×1.25), и цифры двух
 * страниц не сходились. Старые ссылки и закладки ведут туда же.
 */
export default function ForecastPage() {
  redirect('/analysis')
}
