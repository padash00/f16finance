node_modules/
.next/
.env
.env*.local
tsconfig.tsbuildinfo
.claude/
.claude/settings.local.json
__pycache__/
*.py[cod]

.vercel

# приватные выгрузки
*.xlsx

# Apple-приложение (apple/)
.build/
DerivedData/
*.xcuserstate
xcuserdata/
Packages/
.swiftpm/
# Config.xcconfig в репозитории лежит с пустыми ключами как шаблон.
# Локальные боевые ключи держим в Config.local.xcconfig.
apple/Config.local.xcconfig

# Копии, которые создаёт синхронизация папки при конфликте: «AccountSheet 2.swift».
# Xcode и SwiftPM собирают их наравне с оригиналом и падают на дублях типов.
* 2.*
* 3.*

# Служебные файлы Finder.
.DS_Store


# Настройки probe: ключ регистрации и учётные данные устройства.
# В репозиторий не попадают — там секреты остаются навсегда.
tools/arena-probe/probe-settings.local.json
tools/arena-probe/arena-probe.log
tools/arena-probe/senet-scan-report.txt
