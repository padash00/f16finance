'use client'

/**
 * Уведомления — один общий модуль на всё приложение.
 *
 * Здесь лежала ВТОРАЯ копия реализации (своё `memoryState`, свои `listeners`).
 * Смонтированный в `app/layout.tsx` `<Toaster/>` подписан на `@/hooks/use-toast`,
 * поэтому всё, что страницы слали через эту копию, уходило в никуда: на
 * зарплате, прибыльности, отчётах, целях, движении денег и в настройках
 * уведомления просто не появлялись — ни успеха, ни ошибки.
 *
 * Путь оставлен рабочим (на него завязаны импорты страниц), но это теперь
 * реэкспорт настоящего хранилища.
 */

export * from '@/hooks/use-toast'
